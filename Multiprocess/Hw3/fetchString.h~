#include <string.h>
#include <stdio.h>
#include <stdlib.h>

/* Fetch the specific string and return the count */
int fetch_specify_str(char* spec_str,char* source, int size_spec , int start , int end);
/* Test the file length */
int lengthof(char *filename);
/* Get the content */
char *contentof(char *filename,int size);
